<UserControl x:Class="RecipeWPFUI.Views.RecipeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RecipeWPFUI.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <TextBox Height="50" Margin="420,69,10,331" Text="{Binding SearchForDishesFromIngredient, UpdateSourceTrigger=PropertyChanged}"/>
        <ListView Margin="420,124,10,10" Name="dishesListView" ItemsSource="{Binding View}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Dish" DisplayMemberBinding="{Binding DishName, Mode = OneWay}" Width="Auto"/>
                </GridView>
            </ListView.View>
        </ListView>
        <ListView Margin="0,124,430,10" x:Name="cuisinesListView" ItemsSource="{Binding Cuisines}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Type Of Cuisine" DisplayMemberBinding="{Binding TypeOfCuisine, Mode=OneWay}" Width="Auto"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
